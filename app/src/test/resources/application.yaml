telegram:
  bot:
    token: "foo"
    api: "https://api.telegram.org/"
    timeOutSeconds: 2
  cron:
    #every 2 sec
    longPollingConfiguration: "0/2 * * ? * *"
    #at 8:10 daily
    runReminderConfiguration: "0 10 8 ? * *"

spring:
  datasource:
   url: jdbc:tc:postgresql:11://Bot
